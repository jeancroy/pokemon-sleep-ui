import {EventIndex} from '@/ui/info/event/index/main';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({key: 'Info.Event.Index.Title'});

export default EventIndex;
