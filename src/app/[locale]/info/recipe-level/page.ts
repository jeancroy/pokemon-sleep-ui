import {RecipeLevelInfo} from '@/ui/info/recipeLevel/main';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({key: 'Info.RecipeLevel.Title'});

export default RecipeLevelInfo;
