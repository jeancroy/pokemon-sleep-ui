import {PokedexTierList} from '@/ui/pokedex/tier/main';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({key: 'Pokedex.Tier.Title'});

export default PokedexTierList;
