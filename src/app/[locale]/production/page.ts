import {ProductionComparison} from '@/ui/production/main';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({key: 'Production.Title'});

export default ProductionComparison;
